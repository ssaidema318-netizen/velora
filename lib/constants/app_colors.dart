import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ===== Brand =====
  static const Color primary = Color(0xFF0055FF);
  static const Color primaryLight = Color(0xFF4D88FF);
  static const Color primaryDark = Color(0xFF003ECC);

  // ===== Background =====
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundSecondary = Color(0xFFF8F9FB);

  // ===== Surface =====
  static const Color surface = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFF5F5F7);

  // ===== Text =====
  static const Color textPrimary = Color(0xFF1D1D1F);
  static const Color textSecondary = Color(0xFF86868B);
  static const Color textHint = Color(0xFFA1A1AA);
  static const Color textWhite = Colors.white;

  // ===== Border =====
  static const Color border = Color(0xFFE5E7EB);

  // ===== Status =====
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFFCC00);
  static const Color error = Color(0xFFFF3B30);

  // ===== Rating =====
  static const Color star = Color(0xFFFFB800);

  // ===== Flash Deals =====
  static const Color discount = Color(0xFFFF3B30);

  // ===== Icons =====
  static const Color icon = Color(0xFF1D1D1F);
  static const Color iconSecondary = Color(0xFF86868B);

  // ===== Shadow =====
  static const Color shadow = Color(0x14000000);

  // ===== Divider =====
  static const Color divider = Color(0xFFEAEAEA);
}